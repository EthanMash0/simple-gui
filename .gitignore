# build output
build/

# build artifacts (in case anything ends up outside build/)
*.o
*.a
*.so
*.d
*.dSYM/

# binaries (fallback if built at repo root)
hyprdock

# compilation database
compile_commands.json

# caches
.cache/
*.cache

# editor / OS noise
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# Arch packaging outputs (common makepkg artifacts)
pkg/
src/
*.pkg.tar.*
